services:
  postgres:
    image: postgres:18-alpine
    container_name: candid-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: BXxJk6ras6!w
      POSTGRES_DB: candid
    ports:
      - "5431:5432"
    volumes:
      - candid_pgdata:/var/lib/postgresql/data
volumes:
  candid_pgdata:
